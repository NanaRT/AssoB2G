{% extends 'AssoB2GBundle:Default:accueil.html.twig' %}

{%block head%}
	<link rel="stylesheet" href="{{ asset('bundles/assob2g/css/blog-home.css')  }}" />
{%endblock%}

{% block body %}
    <div class="container">

        <div class="row">

            <div class="col-md-8">

                <h1 class="page-header">
                    Evenements
                </h1>
				
				{%for evenement in entities%}
					<section id="{{evenement.systName}}">
						<hr>
					</section>
		                <h2>
		                    <a href="{{path('evenement_show',{'id':evenement.id}) }}">{{evenement.nom}}</a>
		                </h2>
		                <p><span class="glyphicon glyphicon-time"></span> {{ evenement.dateDebut|date("d/m/Y") }} au {{ evenement.dateFin|date("d/m/Y") }} </p>
		                <hr>
		                <img class="img-responsive" src="{{ asset('bundles/assob2g/image/Evenement/'~evenement.systName~'.jpg') }}" alt="{{evenement.nom}}">
		                <hr>
		                {%if evenement.description%}
		                	<p>{{evenement.description}}</p>
		                {%endif%}
		                {#<a class="btn btn-primary" href="{{path('evenement_show',{'id':evenement.id}) }}">Read More <span class="glyphicon glyphicon-chevron-right"></span></a>#}
	            {%endfor%}

                <ul class="pager">
                    <li class="previous">
                        <a href="#">&larr; Older</a>
                    </li>
                    <li class="next">
                        <a href="#">Newer &rarr;</a>
                    </li>
                </ul>

            </div>

            <div class="col-md-4">

                <div class="well">
                    <h4>Blog Categories</h4>
                    <div class="row">
                        <div class="col-lg-12">
                            <ul class="list-group">
                            	{%for evenement in entities%}
							    	<a href="{{path('evenement_show',{'id':evenement.id}) }}">
							    		<li class="list-group-item">
							    			{{evenement.nom}}
							    		</li>
							    	</a>
							    {%endfor%}
							</ul>
                        </div>
                    </div>
                </div>

                <div class="well">
                    <h4>AB2G en quelques mots</h4>
                    <p>
                    	Depuis Juillet 2015, l'Association Bordelaise des Geeks et Gamers participe au développement de l'esport et à la propagation de la culture geek dans la région bordelaise.
						<br>
						De la gestion d'un événement à son animation, l'AB2G couvre un large panel de missions.
						<br>
						Nous sommes également de fervents défenseurs du retour des licornes, notamment dans la rue Sainte Catherine.
					</p>
                </div>
            </div>

        </div>

        <hr>


{%endblock%}
