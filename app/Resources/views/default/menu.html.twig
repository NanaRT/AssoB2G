
<li>
    <a href="{{path('evenement') }}">Nos Evenements</a>
</li>
<li class="dropdown">
	<a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
		<span class="caret"></span>
	</a>
	<ul class="dropdown-menu">
		{%for evenement in queryService.getEvenements()%}
    		<li>
				<a href="{{path('evenement_show', {'id':evenement.id}) }}" >
    				{{evenement.nom}}
    			</a>
    		</li>
    	{%endfor%}
    </ul>
</li>
<li>
    <a href="{{path('membre') }}">Les Membres</a>
</li>
<li>
    <a href="{{path('apropos') }}">A propos</a>
</li>
<li>
    <a href="{{path('contact') }}">Nous Contacter</a>
</li>